{
  "name": "sybil-airdrop",
  "version": "1.0.0",
  "description": "Sybil-resistant airdrop platform on Base with Billions Network integration",
  "scripts": {
    "compile": "npx hardhat compile",
    
    "deploy": "npx hardhat run scripts/deploy.js --network baseMainnet",
    
    "setup-request": "npx hardhat run scripts/setup-request.js --network baseMainnet"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
